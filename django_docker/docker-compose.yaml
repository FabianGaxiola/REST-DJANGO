services:
  web:
    build: .
    command: python manage.py runserver 0.0.0:8500
    volumes:
      - .:/app
    ports:
      - "8500:8500"
    environment:
      - MAILTRAP_USER=e2f7d709fb4653
      - MAILTRAP_PASSWORD=cff6bdb5f8ac8e
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=mydatabase
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSGRES_PORT=5432

volumes:
  postgres_data:
